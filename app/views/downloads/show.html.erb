<div class="container">
	<section class="content">
		<h1 id="title">HIPHOPDX DOWNLOADER</h1>
		<%= render 'form' %>
		<%= render 'download' %>
		<div id="debug_info">
			<h3>Debug Info</h3>
			<%= debug(params) if Rails.env.development? %>
		</div>
	</section>
</div>